- name: install packages using loop
  hosts: all
  become: yes
  tasks:
  - name: install package
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
      enabled: yes

    loop:
    - postfix
    - mysql
    - httpd
    - git
